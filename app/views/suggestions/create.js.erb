// function refreshForm(innerHTML) {
//   const newSuggestionForm = document.getElementById('new-dates-sugg');
//   newSuggestionForm.innerHTML = innerHTML;
// }

function addSuggDates(SuggDatesHTML) {
  const suggestions = document.getElementById('sugg-dates');
  suggestions.insertAdjacentHTML('beforeend', SuggDatesHTML);
}

<%# if @suggestion.errors.any? %>
  // refreshForm('<%= j render "suggestions/form", survey: @survey, suggestion: @suggestion %>');
<%# else %>
addSuggDates('<%= j render "suggestions/show", suggestion: @suggestion %>');
  // refreshForm('<%= j render "suggestions/form", survey: @survey, suggestion: Suggestion.new %>');
<%# end %>

function addSuggPlace(SuggPlaceHTML) {
  const suggPlaces = document.getElementById('sugg-places');
  suggPlaces.insertAdjacentHTML('beforeend', SuggPlaceHTML);
}

addSuggPlace('<%= j render "suggestions/destination", suggestion: @suggestion %>');

function addSuggThematic(SuggThematicHTML) {
  const suggThematics = document.getElementById('sugg-thematics');
  suggThematics.insertAdjacentHTML('beforeend', SuggThematicHTML);
}

addSuggThematic('<%= j render "suggestions/thematic", suggestion: @suggestion %>');
